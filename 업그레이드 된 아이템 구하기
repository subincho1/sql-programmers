# 업그레이드 된 아이템 구하기

SELECT CHILD.ITEM_ID, CHILD.ITEM_NAME, CHILD.RARITY
FROM ITEM_INFO AS PARENT
INNER JOIN ITEM_TREE AS TREE ON PARENT.ITEM_ID = TREE.PARENT_ITEM_ID
INNER JOIN ITEM_INFO AS CHILD ON CHILD.ITEM_ID = TREE.ITEM_ID
WHERE PARENT.RARITY = 'RARE'
ORDER BY CHILD.ITEM_ID DESC;
